<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lx.rx.java.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.PushDemoActivity">
        </activity>
        <activity android:name=".activity.ObserverDemoActivity">
        </activity>
        <activity android:name=".activity.RxJavaActivity">
        </activity>
        <activity android:name=".activity.Rx2ArrayActivity">
        </activity>
        <activity android:name=".activity.ObservableSummaryActivity">
        </activity>

        <activity android:name=".activity.OperatorActivity"/>
    </application>

</manifest>